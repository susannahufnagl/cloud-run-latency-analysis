: "${PROJECT:=project-accountsec}"
: "${ZONE:=europe-west10-b}"
: "${INSTANCE:=ce-latency-bench}"
: "${REGION:=europe-west10}"

: "${CR_BASE:=https://cloudrun-broker-single-go-997595983891.europe-west10.run.app}"
# CE_BASE lassen wir absichtlich weg/kommentiert – wird dynamisch gesetzt

: "${COUNT:=500}"
: "${SLEEP:=0.2}"
: "${TIMEOUT:=15}"

: "${TOPIC_ID:=cloudrun-broker-single}"
: "${PORT:=8080}"
: "${TOPIC:=cloudrun-broker-single}"
: "${PROJECT_ID:=project-accountsec}"




